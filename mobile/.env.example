# Base URL of the Laravel API (no trailing slash).
# When testing on a physical device/emulator, use your machine's LAN IP,
# not "localhost" (e.g. http://192.168.1.20:8000).
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000
